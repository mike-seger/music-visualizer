/** Single source of truth for the application version.
 *  Used as a `?version=` cache-buster on all hash-based resource URLs
 *  (meta.json, presets/<hash>.ext, previews/<hash>.<ext>).
 */
export const visualizerVersion = '1.0.0'
